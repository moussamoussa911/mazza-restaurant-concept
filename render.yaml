services:
  - type: web
    name: mazza-restaurant-concept
    runtime: static
    plan: free
    buildCommand: echo "Static site ready"
    staticPublishPath: .
    branch: main
    autoDeploy: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

